publish:
  pre_action:
    index: 10
    label: "Pre action"
    commands:
      - name: "initalize_action"
        label: "Initialize Action"
        path: "silex_client.commands.log"
        args:
          - "Starting action query"

  action:
    label: "Action"
    index: 20

  post_action:
    index: 30
    label: "Post action"
    commands:
      - name: "close_action"
        label: "Close Action"
        path: "silex_client.commands.log"
        args:
          - "Closing action query"
