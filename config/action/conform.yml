conform:
  steps:
    conform:
      label: "Conform"
      index: 20
      commands:
        conform:
          path: "silex_client.commands.conform.Conform"
          parameters:
            filename: ""