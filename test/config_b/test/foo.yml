foo: !inherit
  name: ".bar"
  key: "bar"
  category: "test"

  steps:
    action:
      commands:
        first_b:
          label: "Publish"
          path: "silex_client.commands.utils.Log"
          parameters:
            message: "FOO action config from path config_b"
            level: "info"
            hide: true
