foo: !inherit
  parent: ".bar"
  category: "test"

  steps:
    action:
      commands:
        first_b:
          label: "Publish"
          definition_path: "silex_client.commands.log.Log"
          inputs:
            message:
              value: "FOO action config from path config_b"
            level:
              value: "info"
          hide: true
