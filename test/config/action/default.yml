publish:
  pre_action:
    index: 10
    label: "Pre action"
    commands:
      - name: "initalize_action"
        label: "Initialize Action"
        path: "silex_client.commands.utils.Log"
        args:
          message: "Starting action query"
        hide: true

  action:
    label: "Action"
    index: 20

  post_action:
    index: 30
    label: "Post action"
    commands:
      - name: "close_action"
        label: "Close Action"
        path: "silex_client.commands.utils.Log"
        args:
          message: "Closing action query"
        hide: true
