test: !inherit
  parent: "./action/default.yml"
  key: "test"

  steps:
    action:
      commands:
        publish:
          label: "Publish"
          path: "silex_client.commands.utils.Log"
          parameters:
            message: "Publishing files"
            level: "info"
            hide: true
