foo: !inherit
  parent: ".bar"
  category: "test"

  steps:
    action:
      commands:
        first_a:
          label: "Publish"
          definition_path: "silex_client.commands.log.Log"
          inputs:
            message:
              value: "FOO action config from path config_a"
            level:
              value: "info"
          hide: true
        second_a:
          label: "Publish"
          definition_path: "silex_client.commands.log.Log"
          inputs:
            message:
              value: "FOO action config from path config_a"
            level:
              value: "info"
          hide: true
