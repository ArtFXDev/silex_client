bar: !inherit
  parent: "foo"

  steps:
    pre_action:
      index: 10
      label: "Pre action"
      commands:
        initalize_action_a:
          label: "Initialize Action"
          definition_path: "silex_client.commands.log.Log"
          inputs:
            message:
              value: "BAR action config from path config_a"
            level:
              value: "debug"
          hide: true

    action:
      label: "Action"
      index: 20

    post_action:
      index: 30
      label: "Post action"
      commands:
        close_action_a:
          label: "Close Action"
          definition_path: "silex_client.commands.log.Log"
          inputs:
            message:
              value: "BAR action config from path config_a"
            level:
              value: "debug"
          hide: true
